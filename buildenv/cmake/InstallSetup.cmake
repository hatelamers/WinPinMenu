
include(GNUInstallDirs)
if(${CMAKE_SYSTEM_NAME} MATCHES Windows)
	set(INSTALL_DOCDIR ${CMAKE_INSTALL_BINDIR})
	set(INSTALL_LOCALEDIR ${CMAKE_INSTALL_BINDIR})
else()
	set(INSTALL_DOCDIR ${CMAKE_INSTALL_DOCDIR})
	set(INSTALL_LOCALEDIR ${CMAKE_INSTALL_LOCALEDIR})
endif()
if(${CMAKE_SYSTEM_NAME} MATCHES Linux)
	set(FD_INSTALL_DESKTOPDIR ${CMAKE_INSTALL_DATADIR}/applications)
	set(FD_INSTALL_ICONSDIR ${CMAKE_INSTALL_DATADIR}/pixmaps)
endif()
